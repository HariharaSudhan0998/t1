pipeline {
    agent any
    stages{
         stage ('Testing') {
             steps{
                 sh '${WORKSPACE}/t1/test.sh'
             //sh('test.sh')
             }
           }
       }
}
